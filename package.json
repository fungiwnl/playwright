{
  "scripts": {
    "test": "yarn playwright test --config=config/playwright.config.ts",
    "test:headed": "yarn playwright test --headed --config=config/playwright.config.ts",
    "test:debug": "yarn playwright test --debug --config=config/playwright.config.ts",
    "test:record": "yarn playwright codegen",
    "showtrace": "yarn playwright show-trace",
    "test:help": "yarn playwright --help"

  },
  "name": "playwright",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:fungiwnl/playwright.git",
  "author": "<brianf.iwnl@gmail.com>",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@playwright/test": "^1.22.2",
    "@types/node": "^18.0.0",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.4"
  }
}
